# Full Spec Acceptance Audit

Date: 2026-07-10

This audit compares the current implementation against `docs/superpowers/specs/2026-07-09-android-offline-assistant-poc-design.md`.

## Proven By Current Evidence

- New Android project exists under `android-offline-assistant-poc`.
- Kotlin + Jetpack Compose app builds with Gradle wrapper.
- Physical Pixel 10 is visible through `adb`.
- Debug APK installs on Pixel 10.
- App launches on Pixel 10.
- `./gradlew test` passes.
- `./gradlew assembleDebug` passes.
- `./gradlew connectedDebugAndroidTest` passes 1 Compose test on Pixel 10.
- `./gradlew connectedDebugAndroidTest` now passes 3 Compose tests on Pixel 10 after adding reference-flow coverage.
- `./gradlew connectedDebugAndroidTest` now passes 5 tests on Pixel 10 after adding persistence and reminder scheduler coverage.
- `./gradlew connectedDebugAndroidTest` now passes 7 tests on Pixel 10 after adding widget-action and microphone permission-card coverage.
- `./gradlew connectedDebugAndroidTest` now passes 8 tests on Pixel 10 after adding the vertical voice pipeline and persisted widget action coverage.
- `./gradlew connectedDebugAndroidTest` now passes 9 tests on Pixel 10 after adding native whisper.cpp JNI coverage for a staged Russian timer WAV.
- `./gradlew connectedDebugAndroidTest` now passes 10 tests on Pixel 10 after adding bundled Whisper model materialization from APK assets into app files.
- Latest stabilization verification after Qwen answer-only reroute, action copy/open-app tests, and real-Whisper ASR debug coverage: `./gradlew test assembleDebug` passes locally, targeted `WhisperNativeSmokeTest` passes 2/2 on Pixel 10, and `./gradlew :app:connectedDebugAndroidTest` passes 28/28 on Pixel 10 with 0 skipped and 0 failures.
- Fresh verification after notification permission stabilization: `./gradlew :app:connectedDebugAndroidTest` passes 29/29 on Pixel 10 with 0 skipped and 0 failures.
- Fresh verification after reminder scheduler wiring and notification acceptance test: `./gradlew :app:connectedDebugAndroidTest` completes successfully on Pixel 10; the normal suite skips the preconditioned notification acceptance test unless `POST_NOTIFICATIONS` is revoked first, and all non-skipped tests pass.
- Direct notification acceptance run after revoking `POST_NOTIFICATIONS` passes `NotificationPermissionAcceptanceTest` on Pixel 10.
- Targeted `MainChatScreenTest` now passes 8/8 on Pixel 10, including `noteEditActionUpdatesPersistedStore`.
- Fresh full connected verification after ASR-fallback cleanup and docs update: `./gradlew :app:connectedDebugAndroidTest` passes on Pixel 10; the preconditioned notification acceptance test is skipped in the normal permission-granted run and all non-skipped tests pass.
- Open-app alternative selection is implemented and targeted-tested: `ChatViewModelFallbackTest.openAppCommandAddsAlternativesWhenSeveralLaunchableAppsMatch` proves the `OpenAppCard` receives `alternatives`, `OpenAppCardRendererTest.alternativeButtonCarriesSelectedPackagePayload` proves choosing an alternative emits the selected package, and `AndroidPlatformAdaptersTest.opensLaunchableAppByPackageNameAndLabel` proves PackageManager lookup finds the app label/package on Pixel 10.
- Android timer/alarm action wiring now exists in the app layer: `ChatViewModel` delegates `set_timer` and `set_alarm` success responses through `PlatformActions.createSystemTimer` / `createSystemAlarm`; `AndroidPlatformAdapters` implements those calls with `AlarmClock.ACTION_SET_TIMER` and `AlarmClock.ACTION_SET_ALARM`; passive timer/alarm cards are returned when Android accepts the action, and `ErrorCard` is returned if the system action fails. The alarm card "open system" action is also wired through `PlatformActions.openSystemAlarms` to `AlarmClock.ACTION_SHOW_ALARMS` instead of only showing local feedback. `ChatViewModelFallbackTest.timerCommandDelegatesToAndroidSystemTimerAndReturnsPassiveCard`, `alarmCommandDelegatesToAndroidSystemAlarmAndReturnsPassiveCard`, `timerCommandReturnsErrorCardWhenAndroidSystemTimerFails`, and `alarmOpenSystemWidgetActionOpensSystemAlarmScreen` cover the ViewModel boundary locally.
- Historical Pixel demo validation exposed and fixed Android Clock integration details: the manifest requests `com.android.alarm.permission.SET_ALARM`, legacy timer/alarm adapters use `EXTRA_SKIP_UI=true`, and `AndroidPlatformAdaptersTest` verifies permission plus extras. `DemoVideoFlowTest` recorded the accepted weather -> passive timer -> note -> local Qwen MP4. The current product timer subsequently moved to an in-app persisted countdown with pause/resume/cancel; alarm delegation remains system-backed.
- Help and clarification widgets now have interactive text insertion. `HelpCardRenderer` emits `help_example` actions with the example text, `ClarificationCardRenderer` emits `clarification_suggestion` actions with the selected suggestion, and `ChatViewModel` inserts that text into the chat input. `ChatViewModelFallbackTest.helpExampleActionInsertsTextIntoInput` and `clarificationSuggestionActionInsertsTextIntoInput` cover this behavior locally.
- Fresh phone demo video evidence for weather, timer, note, and complex local Qwen answer is stored at `build/device-screenshots/offline-assistant-demo-points1-2026-07-09.mp4`.
- Notification permission dialog and post-grant reminder notification video evidence is stored at `build/device-screenshots/offline-assistant-notification-permission-2026-07-09.mp4`.
- Chat input accepts text in the connected Compose test.
- Sending `Поставь таймер на 5 минут` returns assistant text and renders `TimerCard`.
- The main chat shell now exposes the reference navigation chrome: `Assistant` title, menu affordance, privacy status affordance, bottom navigation labels `Чат`, `История`, `Навыки`, `Настройки`, and a separate voice action.
- Sending `Какая погода сегодня?` renders `WeatherCard` and visible `21°`.
- Sending `Посчитай 125 умножить на 37` renders `CalculatorCard` and visible `4625`.
- Sending `Напомни через час проверить духовку` renders `ReminderCard`.
- Core contracts exist: `AssistantResponse`, `ResponseStatus`, `WidgetPayload`, `DebugInfo`, `LatencyBreakdown`.
- Response status serializes as product JSON values such as `success` and `clarification_required`.
- Reusable pure Kotlin skill contracts now exist in `:core`: `NormalizedCommand`, `ClarificationRequest`, `Skill`, `SkillResult`, and `SkillStatus`. `SkillContractsTest.skillReceivesNormalizedCommandAndReturnsStructuredResult` proves a skill receives normalized command data, returns a structured widget result, and serializes `SkillStatus` as stable product JSON values such as `success`.
- Concrete `TimeSkill`, `WeatherSkill`, `TimerSkill`, `AlarmSkill`, `ReminderSkill`, `NoteSkill`, `CalculatorSkill`, `OpenAppSkill`, `HelpSkill` and `UnknownSkill` now live in `:core`. `SkillRegistry` rejects duplicate intent ownership, covers the MVP intent set and is the only normalized-command dispatch path used by `AssistantEngine`; `BuiltInSkillsTest` covers registry completeness and engine dispatch.
- Reusable slot-normalization contracts now exist in `:core`: `SlotNormalizer`, `NormalizationResult`, and `ValidationError`. `SlotNormalizerTest` proves complete timer NLU returns `NormalizedCommand`, while incomplete timer NLU returns a deterministic `ClarificationRequest` with partial slots preserved. `AssistantEngineTest.engineUsesInjectedSlotNormalizerForValidationClarification` proves the high-confidence NLU path now calls the injected normalizer before skill execution.
- Widget registry exists in Compose UI.
- Widget preview screen exists and includes deterministic payloads for all required widget types. `WidgetPreviewScreenTest.sampleWidgetsCoverAllExpectedWidgetTypesInOrder` guards the complete required widget list, and `openAppPreviewIncludesAlternativeSelectionPayload` proves the preview covers OpenApp alternative selection without requiring PackageManager.
- Debug screen exists and shows the latest command history from the shared chat view model.
- Settings screen exists, reports model readiness, exposes a persisted fallback-threshold slider, and provides demo cleanup actions for chat history plus notes/reminders.
- Text-only rule-based NLU stub exists.
- Reusable pure Kotlin intent schema now exists in `:core`: `IntentDefinition` and `IntentSchema.default` define stable intent ids, required slots, optional slots, skill ids, and success widget mappings for the MVP intents. `IntentSchemaTest.defaultIntentSchemaDefinesMvpIntentsAndWidgetMappings` guards the timer/calculator/current-time mappings and verifies coverage of the MVP intent list. `DeterministicSlotNormalizer` reads required slots from this schema instead of keeping a separate hard-coded map.
- Slot extraction exists for timer duration, alarm time, weather location, calculator expression, note text, reminder text, and app name.
- Skills/engine return structured widgets for timer, weather, calculator, alarm, reminder, note, open app, help, clarification, error, and generic fallback paths.
- Core `NoteStore` and `ReminderStore` contracts exist.
- Core in-memory note/reminder stores support create, list, update/complete, and delete operations.
- `AssistantEngine` can be constructed with injected note/reminder stores and persists note/reminder commands into them.
- Android SharedPreferences note/reminder stores persist records across store instances.
- Android reminder scheduler creates a notification channel and schedules alarm-backed reminders without crashing on Pixel 10.
- Timer, reminder, and note widget controls now emit visible chat feedback instead of being no-op buttons.
- Note delete and reminder complete actions mutate the SharedPreferences-backed stores from rendered widget payloads in connected tests.
- Missing `RECORD_AUDIO` permission now produces a `PermissionCard`; its allow action is connected to the Android runtime permission launcher.
- Missing `POST_NOTIFICATIONS` for reminder commands now produces a `PermissionCard`; its allow action carries the requested permission through the widget payload instead of hard-coding microphone permission.
- Reminder notification scheduling is wired from successful `ReminderCard` responses through `ChatViewModel` and `AndroidPlatformAdapters` into `ReminderNotificationScheduler`.
- On Pixel 10, `NotificationPermissionAcceptanceTest` proves the app can request `POST_NOTIFICATIONS` through the Android system dialog, then schedule and display a local reminder notification after grant.
- The app now includes an `AudioRecord`-based recorder that captures 16 kHz mono PCM and writes a WAV file suitable for whisper.cpp input.
- Recorder-generated cache WAV files are deleted after sync or async voice processing. `ChatViewModelFallbackTest.voiceRecordingDeletesRecorderCacheFileAfterProcessing` and `asyncVoiceRecordingDeletesRecorderCacheFileAfterProcessing` cover the default privacy behavior while staged/eval fixtures are left alone.
- A unit-tested `VoiceCommandPipeline` converts ASR transcript text into the existing assistant engine path; the proven sample is `Поставь таймер на 5 минут` -> `set_timer` -> `TimerCard`.
- The debug APK includes native `arm64-v8a` libraries built from vendored `whisper.cpp` and a JNI wrapper at `app/src/main/cpp/native_whisper.cpp`.
- On Pixel 10, native `WhisperTranscriber` loads the app-file Whisper model and transcribes `docs/testing/audio/timer-command.wav`; the transcript then produces `TimerCard`.
- On Pixel 10, `WhisperNativeSmokeTest.nativeWhisperVoiceRecordingAddsAsrLatencyToDebugHistory` proves the real Whisper -> `ChatViewModel.handleVoiceRecording` path records the voice transcript and ASR latency in debug history.
- On Pixel 10, `WhisperNativeSmokeTest.nativeWhisperWritesFixedAsrEvaluationArtifact` writes `files/whisper-asr-eval.jsonl`; the latest artifact for `timer-command.wav` recorded transcript `поставь таймер на 5 минут.`, ASR latency `2499 ms`, keyword hit `true`, intent `set_timer`, and widget `timer_card`.
- The broader manifest-driven ASR eval passed on Pixel 10 for all `4/4` weather, timer, calculator and note fixtures with both models. Base Q5_1 measured `3490-3828 ms` (median `3634.5 ms`); selected Small Q5_1 measured `8305-15410 ms` (median `13615.5 ms`, mean `12736.5 ms`) with peak process memory around `450 MB PSS` / `537 MB RSS`. Small rendered `18*3` instead of the spoken word `умножить`, while generic NLU still produced the expected `calculate` intent and `CalculatorCard`.
- Human phone testing confirmed the live voice path executes correctly when Whisper produces an accurate transcript; observed inaccurate transcripts remain visible to the user and are not patched through one-off ASR phrase fallbacks.
- The debug APK bundles `models/whisper/whisper-small-multilingual-q5_1.bin` and `ModelReadinessRepository` materializes it into `filesDir`, giving whisper.cpp a normal file path.
- The debug APK now passes 16 KB native-library packaging checks: `zipalign -c -P 16` succeeds, and all packaged `arm64-v8a` shared libraries report `LOAD` segment alignment `0x4000`.
- ONNX Runtime Android `1.27.0` is linked into the app and packaged with 16 KB-compatible `arm64-v8a` native libraries.
- A core `NluParser` interface now lets `AssistantEngine` use injected NLU implementations instead of being hard-wired to the rule-based stub.
- `OnnxRubertNlu` now has a production ONNX Runtime runner path, a testable runner interface, safe fallback to `RuleBasedNlu`, and chat wiring through `ChatViewModelFactory`.
- RuBERT model readiness now requires a complete ONNX/tokenizer/label bundle before the ONNX path is used.
- `training/rubert/train_export.py` fine-tunes `cointegrated/rubert-tiny2` for intent classification and exports the ONNX/tokenizer/label bundle.
- The spec-facing training layout now exists: `training/data/synthetic_intents.jsonl`, `training/data/eval_set.jsonl`, `training/scripts/generate_synthetic_dataset.py`, `training/scripts/train_rubert_tiny2.py`, `training/scripts/export_onnx.py`, `training/scripts/evaluate_nlu.py`, and `training/README.md`. `training/test_training_layout.py` validates the layout, eval-set slot records, and required metric names.
- On Pixel 10, `OnnxRubertNativeSmokeTest.stagedRubertOnnxRunsOnDeviceCpuAndSelectsMvpIntents` runs the exported RuBERT-tiny2 ONNX bundle locally on CPU and selects timer, weather, calculator, reminder, and note intents above the execution threshold.
- On Pixel 10, `OnnxRubertNativeSmokeTest.stagedRubertOnnxReturnsNormalizedSlotForWordDuration` proves the Android RuBERT runner returns `duration_seconds=120` for `Поставь таймер на две минуты` through the ONNX path with local slot-normalization merge.
- On Pixel 10, `RubertCommandEvaluationTest.stagedRubertDrivesFixedActionCommandSetThroughAssistantEngine` proves the staged RuBERT ONNX bundle drives the assistant engine for current time, weather, timer, timer clarification, alarm, note, reminder, calculator, open app, and help contracts.
- On Pixel 10, `RubertCommandEvaluationTest.stagedRubertDrivesBroaderParaphraseSetThroughAssistantEngine` proves the staged RuBERT ONNX bundle plus deterministic slot normalization drives the expanded action/help paraphrase set through `AssistantEngine`; `broaderRubertParaphraseSetContainsOnlyActionAndHelpCommands` keeps local-LLM answer cases out of the RuBERT command suite.
- On Pixel 10, `RubertSlotEvaluationTest.stagedRubertTokenClassifierWritesSlotOnlyEvaluationArtifact` runs a slot-only evaluation with `useFallbackSlotMerge=false` and writes JSONL rows to saved logcat. The original strict gate reproduced the token-classifier gap at 0/8 exact slot matches because the staged ONNX only exposed `intent_logits`; after re-exporting a joint intent+slot model and fixing standalone `час` normalization, the targeted device slot eval passed 1/1 with 8/8 exact matches on that staged bundle.
- The final RuBERT bundle passed the connected slot-only artifact gate `8/8` for timer durations, alarm times and calculator expressions, and the connected action/help command suite passed on Pixel 10.
- A reproducible host RuBERT export gate now exists at `training/rubert/evaluate_export.py`. It consumes all 12 rows in `training/data/eval_set.jsonl`, writes per-case JSONL and an adjacent aggregate metrics JSON. The 2026-07-10 run reports intent accuracy `1.000`, macro F1 `1.000`, slot precision `0.828`, slot recall `0.923`, slot F1 `0.873`, validation pass rate `0.917`, exact matches `11/12`, and strict regression gate `9/9`; the note slot-label miss remains explicit.
- The stable wrapper `training/scripts/evaluate_nlu.py` delegates to the current host export gate and keeps the spec command name available for future metric expansion.
- Android RuBERT tokenization now preserves model case-sensitivity, which keeps reminder confidence above the fallback threshold.
- RuBERT external staging refresh compares file contents, so same-size tokenizer/label/model updates replace stale app-file bundles.
- Core local-answer contracts now include `LocalAnswerProvider`, `StreamingLocalAnswerProvider`, `LocalAnswerResult`, and `LocalAnswerFallbackParser` in addition to the legacy `FallbackParser` bridge. `LocalAnswerProviderTest` proves the adapter returns `FallbackKind.ANSWER`/`ERROR` only, preserves latency, leaves `intent` null, and leaves `slots` empty.
- Fallback command execution is explicitly blocked: `FallbackKind.COMMAND` is ignored for Android actions and renders a safe generic answer path instead.
- Android `LlamaCppFallbackParser` implements `StreamingLocalAnswerProvider`, builds a plain Russian answer prompt, explicitly avoids structured command JSON instructions, strips thinking output for streaming, and sanitizes accidental JSON so raw model JSON is not shown to users. `LlamaCppFallbackParserTest.llamaParserExposesAnswerOnlyProviderContract` proves it is usable through the answer-only contract.
- Missing or failed local LLM fallback now produces a user-visible `ErrorCard` instead of a fake generic answer. The error payload includes a recoverable settings suggestion, `ErrorCardRenderer` renders suggestions as actions, and the main chat can switch to Settings from the "Открыть настройки" recovery action.
- `MainChatScreen` wires Qwen readiness into `ChatViewModelFactory`; the local answer provider fails safely when the native bridge or GGUF model is unavailable.
- The debug APK now packages `libofflineassistant_llama.so`, built from the vendored llama.cpp/talk-llama sources.
- On Pixel 10, `LlamaNativeSmokeTest.nativeLlamaGeneratesTextFromInstalledGguf` loads the exact staged Qwen2.5 0.5B Instruct Q4_K_M GGUF through JNI and returns non-empty generated text.
- On Pixel 10, `LlamaNativeSmokeTest.llamaFallbackParserRunsThroughJniEngineWithoutCrashing` runs the Qwen answer provider through the JNI engine without a native crash.
- On Pixel 10, `LlamaNativeSmokeTest.llamaFallbackParserReturnsGenericAnswerForComplexQuestion` proves a complex general question routes through local Qwen and returns a safe text answer.
- `LlamaNativeSmokeTest.llamaFallbackParserReturnsPlainAnswerForActionLikeUnknownInsteadOfStructuredCommand` proves action-like unknown text returns plain answer data, not a structured command.
- On Pixel 10, `LlamaNativeSmokeTest.nativeLlamaCanWarmUpInstalledGgufBeforeGeneration` proves the app can warm up Qwen before generation.
- `OfflineAssistantApp` starts staged background Qwen warm-up when the GGUF is ready; native llama.cpp keeps the model and one serialized `32768` context loaded, clears llama memory between requests, and releases the context on Android memory pressure for later foreground rewarm.
- Qwen keeps its `32768` context and `8192` generation ceiling. The prompt requests a completed 2-4 sentence answer, while the shared native `answer_stop_policy.h` stops at semantic boundaries, applies a 900-character emergency cap and ends generation after 90 seconds. `scripts/test_qwen_generation_policy.py` executes the pure C++ policy on the host, and `assembleDebug` compiles the same policy into JNI.
- The JNI llama prompt decode path handles prompts longer than `n_batch` by decoding in chunks, avoiding the prior `llama_context::decode` abort.
- On Pixel 10, `QwenAnswerEvaluationTest.localQwenAnswersFixedComplexQuestionSetWithLatencyArtifact` evaluated the original eight fixed complex/general questions through the real Qwen GGUF and wrote JSONL latency lines to the saved instrumentation logcat. That run passed 1/1 with 0 failures/errors/skips in 189.418s test time.
- The shared Qwen answer-quality eval passed all 12 real GGUF cases on Pixel 10. The saved artifact has plain streamed answers and measured latency `11821-22872 ms`, median `13369 ms`, mean `14710 ms`; quality/repeatability are accepted, while the `<10 s` performance target remains open.
- The latest Qwen eval recorded streamed, non-empty plain-text answers with no JSON, `<think>`, markdown fences/emphasis, CJK leakage, or prompt-instruction leakage. Recorded latencies were 14708 ms, 10215 ms, 10699 ms, 66913 ms, 55061 ms, 12546 ms, 9671 ms, and 7871 ms.
- Qwen answer prompting now includes targeted safety guidance for offline fresh-data questions, guaranteed weight-loss/health questions, and medical reliance questions; the fixed eval proves it answers that fresh weather cannot be known offline, weight loss cannot be guaranteed, and small local models should not be trusted alone for medical questions.
- `ChatViewModelFallbackTest.asyncTextFallbackFailureRendersErrorAndUnblocksUi` proves unexpected async fallback/LLM failures render an `ErrorCard` and clear `isProcessing` instead of leaving the chat blocked.
- On Pixel 10, `QwenUiSmokeTest.repeatedComplexQuestionsRenderTwoLocalLlmAnswerCards` proves two complex questions sent through the real Compose UI render two local-LLM `GenericAnswerCard` responses without a repeat-call crash.
- A fixed core command evaluation set exists at `core/src/test/resources/eval/command_eval.jsonl` and is enforced by `CommandEvaluationSetTest`; it now covers 25 command/paraphrase cases across current time, weather, timer, timer clarification, alarm, alarm clarification, note, reminder, calculator, open app, help, and unknown/generic answer contracts.
- `MainChatScreenTest.debugTabShowsLatestCommandDiagnostics` proves that after a command the `История` tab shows transcript, intent, fallback usage, action result, slot keys, normalized command, and stage latency labels.
- `ModelRuntimeTelemetryStore` records the latest operation, success/failure, latency and error for Whisper transcription, RuBERT inference, and Qwen warm-up/generation. `SettingsScreen` and `DebugScreen` display this persisted runtime state separately from file readiness; unit tests cover each adapter and `DebugScreenTest` covers rendering.
- Internal navigation is gated: debug builds expose `Чат`, `История`, `Навыки`, and `Настройки`, while release navigation hides the debug/history and widget-preview tabs. `OfflineAssistantNavigationTest` covers both tab sets and `:app:compileReleaseKotlin` passes with `BuildConfig.DEBUG` gating enabled.
- `AssistantEngineTest.debugInfoIncludesPerStageLatencyForDirectCommand` and `debugInfoIncludesFallbackLatencyWhenLocalLlmIsUsed` prove core debug latency now includes NLU, fallback LLM, normalization, skill execution, and total timings where applicable.
- `AssistantEngineTest.debugNormalizedCommandUsesNormalizerOutput` proves debug `normalizedCommand` reflects the normalized command data produced by `SlotNormalizer`, not just the raw NLU slots.
- Latency logging across the full pipeline is covered by complementary evidence: `WhisperNativeSmokeTest.nativeWhisperVoiceRecordingAddsAsrLatencyToDebugHistory` proves ASR latency is merged into debug history, `AssistantEngineTest` proves NLU/fallback LLM/normalization/skill/total timings, `MainChatScreenTest.debugTabShowsLatestCommandDiagnostics` proves debug UI labels are visible, and `QwenAnswerEvaluationTest` writes real Qwen latency artifacts from Pixel 10.
- Reinstalling the latest debug APK and launching MainActivity on Pixel 10 shows the app UI without the Android App Compatibility 16 KB dialog.
- Weather payload creation now uses the pure Kotlin `WeatherProvider` contract in `:core`. The default PoC provider is `MockWeatherProvider`, payloads keep a visible `source` label such as `mock`/`cache`/`online`, and `AssistantEngineTest.weatherCommandUsesInjectedWeatherProvider` proves a cache-style provider can be injected without changing Android UI or engine skill routing.
- Product mock references are stored under `docs/design/references/` and referenced by the implementation plan.
- Fresh debug APK with async voice handling is installed and launched on Pixel 10; UIAutomator sees `Assistant`, welcome text, `Mic`, input, and `Send`, with no `Android App Compatibility` dialog.
- Fresh full connected suite after async voice and Qwen repeatability changes passed on Pixel 10: `./gradlew :app:connectedDebugAndroidTest` finished 35 tests with `BUILD SUCCESSFUL`; two permission acceptance tests remain preconditioned and skipped in the normal permission-granted run.
- Targeted no-Wi-Fi/cellular verification passed on Pixel 10 after `adb shell svc wifi disable` and `adb shell svc data disable`: `WhisperNativeSmokeTest.nativeWhisperWritesFixedAsrEvaluationArtifact`, `RubertCommandEvaluationTest.stagedRubertDrivesBroaderParaphraseSetThroughAssistantEngine`, and `QwenUiSmokeTest.repeatedComplexQuestionsRenderTwoLocalLlmAnswerCards` completed with `BUILD SUCCESSFUL`. This was followed by the stricter airplane-mode/no-VPN proof below.
- Strict airplane-mode/no-VPN verification passed on Pixel 10 after enabling airplane mode and temporarily disabling WireGuard: `airplane_mode_on=1`, disabled `com.wireguard.android` package count `1`, active `NetworkAgentInfo{...}` line count `0`, and the targeted local ASR/RuBERT/Qwen connected tests completed 3/3 with `OK (3 tests)`.
- Post-offline manual-test setup restored airplane mode to `0`, re-enabled WireGuard, reinstalled the debug APK, copied Qwen into app files, granted microphone/notification permissions, and launched `com.offlineassistant.poc.debug/com.offlineassistant.app.MainActivity`; UIAutomator sees `Assistant`, welcome text, `Mic`, input, and `Send`.
- Blue Reference UI polish is implemented for the theme, navigation, chat composer, message bubbles, Settings, and result widgets. The 2026-07-10 Pixel regression passed `MainChatScreenTest` plus `SettingsScreenTest` 10/10, including hidden raw chat diagnostics, responsive widget actions, and the persisted ASR selector. Fresh screenshots live under `build/device-screenshots/offline-assistant-ui-polish-*-2026-07-10.png`.
- Fresh post-polish debug APK is installed and launched on Pixel 10 with Qwen/RuBERT/Whisper models in app files; UIAutomator sees `Assistant`, one welcome bubble, `Mic`, input, and `Send`, and does not report `Android App Compatibility` or raw `debug:` text. Screenshot evidence is stored at `build/device-screenshots/offline-assistant-blue-reference-ui-2026-07-09.png`.
- Linux CLI is not implemented.
- Core logic is separated into a pure Kotlin `:core` module and Android UI/platform code in `:app`; reusable skill-interface contracts now live under `com.offlineassistant.core.skills`.
- Connected model-evaluation rows are logged with stable tags and collected immediately after the producing instrumentation run. The host no longer relies on app-private JSONL surviving later instrumentation APK teardown.

## Implemented With PoC Constraints

- `MainChatScreen`: implemented with chat list, text input, send button, real microphone recording, local transcript preview, async processing/streaming state, assistant bubble and widget card. Raw debug diagnostics are intentionally absent from the chat surface.
- `DebugScreen`: implemented with shared chat debug history, model file readiness, persisted model runtime telemetry, NLU/local-answer/normalization/skill/ASR diagnostics and stage latency fields. It is hidden from normal release navigation.
- `SettingsScreen`: implemented with ASR selection, model file/runtime status, automatic-speech control, device permission/storage/memory diagnostics, an interactive persisted fallback-threshold slider that is read by `ChatViewModel` without ViewModel recreation, and cleanup actions for chat history plus notes/reminders.
- `WidgetPreviewScreen`: implemented in-app for internal/debug navigation; no separate design-mode tooling beyond the screen.
- Timer and alarm: structured cards and Android system delegation are wired and physically verified. Timer/alarm creation uses the Clock API permission and skip-UI extra so the assistant remains foregrounded and renders a passive card.
- Reminder and note: cards exist; local persistence is implemented through SharedPreferences-backed stores. Reminder complete, note edit, note delete, and note/calculator copy actions are wired from rendered widget payloads. Note edit uses the chat input as the edit surface and updates the existing persisted note.
- Open app: card exists; Android adapter can launch by package or app label; multiple PackageManager matches are returned as `alternatives` and rendered as explicit choice buttons.
- Voice path: microphone UI now starts/stops real `AudioRecord` capture and routes the resulting WAV through native whisper.cpp into the assistant pipeline; recorder-generated cache WAV files are removed after processing, the Whisper model is bundled and materialized from APK assets, and staged-WAV ASR latency is visible in debug history.
- Voice UI responsiveness: stopping a recording now starts an async voice pipeline. The UI immediately shows local recognition/processing state; after transcript is available it appends the voice user bubble and uses the same streaming Qwen token path as typed text for complex questions.
- RuBERT NLU path: ONNX Runtime adapter, Android wiring, training/export script, staged exported bundle, on-device CPU intent inference, token-classification output decoding, and normalized slot return through the Android runner are proven. Local slot normalization still fills missing keys when the token-classifier output is incomplete.
- Qwen local answers: answer-only prompt, engine routing, UI wiring, llama.cpp JNI, exact Qwen2.5 0.5B Instruct GGUF loading, safe real-model answer fallback, missing-model/error-card recovery, non-thinking streamed answers, app-scope background native model warm-up, runtime telemetry, semantic stop/timeout policy, async error unblocking, non-execution of LLM command output, and sequential streaming/UI requests are covered. The connected 12-case gate passes at `11821-22872 ms`; the `<10 s` target remains an optimization.
- Notification permission flow: reminder responses are gated in the Android view model when `POST_NOTIFICATIONS` is missing, and the generic permission-card action requests the permission named by the widget payload.
- Post-grant reminder notification behavior: successful reminder responses now schedule local notifications through the Android platform adapter, and a preconditioned device acceptance test verifies the notification appears.
- Note edit: `ChatViewModelFallbackTest.noteEditActionLoadsExistingNoteIntoInputAndSendUpdatesPersistedNote` proves edit action loads the stored note into input and `Send` updates the same persisted note with an updated `NoteCard`. `MainChatScreenTest.noteEditActionUpdatesPersistedStore` covers the Compose path and passes on Pixel 10.

## Final Definition of Done Evidence

- Historical product-level live-human microphone evidence is complete. The recorded flow produced transcript `поставь в таймер на 5 минут`, intent `set_timer`, confidence `0.9830345225840099`, source `RUBERT_TINY2`, fallback `false`, passive system `TimerCard`, ASR `1975 ms`, NLU `618 ms`, skill `1 ms` and total `2594 ms`. The later in-app timer migration is covered by unit/UI action tests; the user will repeat the live human voice check as the final manual gate.
- `scripts/live_voice_acceptance.sh` accepts both controllable timer copy and the spec-valid passive system timer result, scrolls Debug history before collecting latency fields, and validates the MP4, screenshot, chat/debug UI dumps, logcat and RuBERT host artifacts.
- `scripts/device_smoke_test.sh` now runs the RuBERT/Qwen/Whisper producers separately and extracts each tagged JSONL immediately, preventing logcat eviction reproduced by the first combined run. The final bundle contains host metrics, RuBERT `8/8`, Qwen `12/12`, Whisper `4/4`, offline deny evidence, permission gates, accepted demo video, screenshot and logcat; `verify_device_smoke_artifacts.py` accepts it.
- A single final-acceptance entrypoint exists at `scripts/run_full_acceptance.sh`. Its host preflight writes RuBERT per-case and aggregate artifacts, runs the native Qwen stop-policy test, script/static verifier tests, design checks and Gradle unit/debug/release builds. It then requires a connected device and runs device smoke plus live voice acceptance. `--host-only` proves host readiness but intentionally does not satisfy product DoD.
- The final runner also supports `scripts/run_full_acceptance.sh --wait-for-device`, which runs host preflight immediately and then waits for adb to report a connected device before starting the phone acceptance stages. This reduces the manual gap while the phone is disconnected, but the final DoD still requires the connected stages and artifacts.
- `scripts/acceptance_status.py build/device-smoke` reports `host_preflight`, `device_smoke` and `live_voice` as present and returns `"complete": true`.
- The final DoD matrix is machine-readable at `docs/acceptance/final-dod-gates.json`; `scripts/final_dod_status.py --artifact-dir build/device-smoke` reports all 40 requirements proven and `"complete": true`.

## Local Model Assets

The accepted Whisper variants are packaged under `app/src/main/assets/models/`. Historical copies discovered in unrelated local projects are not repository dependencies.

- Qwen exact GGUF: `models/external/qwen2.5-0.5b-instruct-gguf/qwen2.5-0.5b-instruct-q4_k_m.gguf`
- Qwen exact GGUF on device: `/data/local/tmp/offline-assistant-qwen.gguf`

Generated RuBERT files live under ignored `models/generated/rubert/` and are staged to `/data/local/tmp/offline-assistant-rubert` for device smoke tests.

## Current Conclusion

The implementation completes all six milestones: Android chat/widget surfaces, reusable concrete skills and registry dispatch, structured responses, RuBERT joint ONNX plus aggregate metrics, native Whisper, Qwen answer-only routing/streaming, app-scope warm-up, persisted model runtime telemetry, native answer stop/timeout safeguards, UI polish and evaluation evidence. Physical-device runs prove the vertical paths, permissions, repeat generation, offline execution and live-human voice flow.

The functional design spec and Definition of Done are complete. Repository, host, offline device-model, permission, action, UI, demo and live-human voice evidence are present and machine-verified. Qwen `<10 s` and the batch Whisper `<3 s` targets remain measured post-DoD optimization work.
