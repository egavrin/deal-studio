# Device Smoke Results

Status: passed on physical Pixel 10; documented latency constraints remain optimization work.

Run date: 2026-07-09 through 2026-07-10

Commands to verify:

- `./gradlew test`
- `./gradlew assembleDebug`
- `adb install -r app/build/outputs/apk/debug/app-debug.apk`
- `adb shell monkey -p com.offlineassistant.poc.debug 1`
- `./gradlew connectedDebugAndroidTest`

Observed results:

- `./gradlew test`: passed.
- `./gradlew assembleDebug`: passed; debug APK produced at `app/build/outputs/apk/debug/app-debug.apk`.
- `adb install -r app/build/outputs/apk/debug/app-debug.apk`: passed on Pixel 10.
- `adb shell monkey -p com.offlineassistant.poc.debug 1`: launched app on Pixel 10.
- `./gradlew connectedDebugAndroidTest`: passed 1 Compose test on Pixel 10 after keyboard focus handling was fixed.
- Screenshot captured to `docs/testing/offline-assistant-poc.png`.
- Later reference-flow run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 3 Compose tests on Pixel 10.
- Fresh reference-shell screenshot captured to `docs/testing/offline-assistant-poc-reference-shell.png`.
- Persistence/scheduler run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 5 Compose/instrumentation tests on Pixel 10.
- Widget-action/permission run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 7 Compose/instrumentation tests on Pixel 10.
- Latest installed build screenshot captured to `docs/testing/offline-assistant-poc-actions-permission.png`.
- Vertical voice pipeline run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 8 Compose/instrumentation tests on Pixel 10.
- Latest voice-pipeline build screenshot captured to `docs/testing/offline-assistant-poc-voice-pipeline.png`.
- Native Whisper run: after staging `whisper-base-multilingual-q5_1.bin` and `timer-command.wav` under `/data/local/tmp`, `./gradlew test assembleDebug connectedDebugAndroidTest` passed 9 connected tests on Pixel 10 with 0 skipped.
- Latest native-Whisper build screenshot captured to `docs/testing/offline-assistant-poc-whisper-native.png`.
- Bundled Whisper model run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 10 connected tests on Pixel 10 with 0 skipped.
- Latest bundled-Whisper build screenshot captured to `docs/testing/offline-assistant-poc-bundled-whisper.png`.
- 16 KB native alignment run: `zipalign -c -P 16 -v 4 app/build/outputs/apk/debug/app-debug.apk` passed, and every `lib/arm64-v8a/*.so` in the APK reports `LOAD` segment alignment `0x4000` through NDK `llvm-readelf`.
- After reinstalling the aligned debug APK on Pixel 10, the Android App Compatibility warning dialog was no longer visible; screenshot captured to `docs/testing/offline-assistant-poc-16kb-aligned.png`.
- ONNX Runtime Android integration run: `./gradlew test assembleDebug connectedDebugAndroidTest` passed 11 connected tests on Pixel 10 with 0 skipped.
- The APK now packages `libonnxruntime.so` and `libonnxruntime4j_jni.so`; both report `LOAD` segment alignment `0x4000`, and `zipalign -c -P 16` still passes.
- Latest ONNX Runtime build screenshot captured to `docs/testing/offline-assistant-poc-onnx-runtime-launched.png`.
- Qwen answer-only contract run: core tests now prove low-confidence/unknown NLU can route through Qwen for text answers, `FallbackKind.COMMAND` does not execute Android actions, and Qwen answers render `GenericAnswerCard`.
- Android Qwen parser tests now validate plain-answer prompts, absence of structured command JSON instructions, safe text answer parsing, streaming think-tag filtering, accidental JSON sanitization, and missing-model safe failure.
- Fresh fallback-routing verification: `./gradlew test assembleDebug` passed, repeated `./gradlew :app:connectedDebugAndroidTest` passed 11/11 tests on Pixel 10, and the updated APK installed/launched successfully.
- Latest fallback-routing build screenshot captured to `docs/testing/offline-assistant-poc-fallback-routing.png`.
- Native llama.cpp JNI run: `libofflineassistant_llama.so` is now built from the vendored llama.cpp/talk-llama sources and packaged in the debug APK.
- Exact Qwen GGUF staging run: `models/external/qwen2.5-0.5b-instruct-gguf/qwen2.5-0.5b-instruct-q4_k_m.gguf` was staged to `/data/local/tmp/offline-assistant-qwen.gguf`; the device smoke copies it into app files as `models/qwen/qwen2.5-0.5b-instruct.gguf` when needed.
- `LlamaNativeSmokeTest.nativeLlamaGeneratesTextFromInstalledGguf` passed on Pixel 10 and proves JNI model loading plus token generation returns non-empty text.
- `LlamaNativeSmokeTest.llamaFallbackParserRunsThroughJniEngineWithoutCrashing` passed on Pixel 10 and proves the answer-only Qwen provider can call the JNI engine without a native crash.
- Earlier verification: `./gradlew test assembleDebug connectedDebugAndroidTest` passed on Pixel 10 with 13 connected tests and 0 failures.
- Current debug APK passes `zipalign -c -P 16 -v 4`; a readonly ELF header check reports every packaged `lib/arm64-v8a/*.so` `LOAD` segment aligned to `0x4000`, including `libofflineassistant_llama.so`.
- After reinstalling the latest APK, Pixel 10 launches `com.offlineassistant.poc.debug/com.offlineassistant.app.MainActivity` without the Android App Compatibility 16 KB warning dialog; screenshot captured to `build/device-screenshots/main-after-tests.png`.
- Exact Qwen2.5 0.5B Instruct GGUF run: `qwen2.5-0.5b-instruct-q4_k_m.gguf` is staged at `/data/local/tmp/offline-assistant-qwen.gguf`, materialized into app files as `models/qwen/qwen2.5-0.5b-instruct.gguf`, and covered by `LlamaNativeSmokeTest`.
- RuBERT-tiny2 training/export run: `training/rubert/train_export.py` produced `rubert-tiny2-intent-slots.onnx`, `vocab.txt`, `intent_labels.txt`, and `slot_labels.txt`; the bundle is staged at `/data/local/tmp/offline-assistant-rubert` and materialized into app files.
- `OnnxRubertNativeSmokeTest.stagedRubertOnnxRunsOnDeviceCpuAndSelectsMvpIntents` passed on Pixel 10 and covers timer, weather, calculator, reminder, and note intents with confidence above the execution threshold.
- `OnnxRubertNativeSmokeTest.stagedRubertOnnxReturnsNormalizedSlotForWordDuration` passed on Pixel 10 and covers `Поставь таймер на две минуты` -> `duration_seconds=120` through the Android RuBERT runner.
- `RubertCommandEvaluationTest.stagedRubertDrivesFixedActionCommandSetThroughAssistantEngine` passed on Pixel 10 and covers current time, weather, timer, timer clarification, alarm, note, reminder, calculator, open app, and help through the real RuBERT ONNX path.
- `RubertCommandEvaluationTest.stagedRubertDrivesBroaderParaphraseSetThroughAssistantEngine` passed on Pixel 10 and covers the expanded action/help command/paraphrase set through staged RuBERT ONNX + `AssistantEngine`. Action/help cases are required to keep debug `nluSource=RUBERT_TINY2`; local Qwen answer cases are covered separately by Qwen eval/UI tests.
- Slot-only RuBERT eval baseline: `RubertSlotEvaluationTest.stagedRubertTokenClassifierWritesSlotOnlyEvaluationArtifact` originally passed only as an artifact writer with `useFallbackSlotMerge=false`; its saved summary was `{"cases":8,"exact_matches":0,"uses_fallback_slots":false}` and exposed that the staged bundle was effectively intent-only for slots. The eval is now a strict gate, and later RuBERT work below improves the export/training path.
- The Android RuBERT tokenizer now preserves case to match the model tokenizer; this fixed reminder confidence dropping below the fallback threshold.
- RuBERT staging refresh now replaces same-size changed files by comparing content, not only file length.
- Real Qwen answer run: `LlamaNativeSmokeTest.llamaFallbackParserReturnsGenericAnswerForComplexQuestion` passed on Pixel 10 with Qwen2.5 0.5B Instruct, native llama context `32768`, answer generation cap `8192`, non-empty streaming output, and no visible JSON/`<think>` text.
- Qwen answer-only stabilization: action-like unknown text is covered by `LlamaNativeSmokeTest.llamaFallbackParserReturnsPlainAnswerForActionLikeUnknownInsteadOfStructuredCommand`; Qwen must not generate structured command JSON and its output must not execute Android actions.
- Local answer contract stabilization: `LocalAnswerProviderTest` proves the core answer-only adapter maps local model answers/errors into fallback results without command intent/slots, and `LlamaCppFallbackParserTest.llamaParserExposesAnswerOnlyProviderContract` proves the Android llama.cpp adapter implements `StreamingLocalAnswerProvider`.
- Qwen quality/latency eval run: `QwenAnswerEvaluationTest.localQwenAnswersFixedComplexQuestionSetWithLatencyArtifact` passed on Pixel 10 against eight fixed complex/general questions. The latest XML report shows 1 test, 0 failures, 0 errors, 0 skipped, and 189.418s test time. The saved logcat artifact records JSONL rows for all eight questions with latencies 14708 ms, 10215 ms, 10699 ms, 66913 ms, 55061 ms, 12546 ms, 9671 ms, and 7871 ms.
- Qwen safety prompt tuning from that eval: the initial prompt incorrectly answered that fresh weather could be obtained without internet, over-promised weight-loss likelihood, and later copied an internal medical prompt phrase. The current prompt scopes examples only to relevant policy questions; the passing artifact answers that fresh weather cannot be known offline, weight loss cannot be guaranteed, and medical questions should not rely only on a small local model. The test now rejects JSON/`<think>`/markdown fences/emphasis, CJK leakage, and prompt-instruction leakage in both final and streamed answers.
- Latency logging coverage: `WhisperNativeSmokeTest.nativeWhisperVoiceRecordingAddsAsrLatencyToDebugHistory` proves ASR latency is merged into debug history; `AssistantEngineTest.debugInfoIncludesPerStageLatencyForDirectCommand` and `debugInfoIncludesFallbackLatencyWhenLocalLlmIsUsed` prove NLU, fallback LLM, normalization, skill, and total timings; `MainChatScreenTest.debugTabShowsLatestCommandDiagnostics` proves the debug UI exposes the stage labels; `QwenAnswerEvaluationTest` records real Qwen latency JSONL artifacts on Pixel 10.
- Normalized-command debug coverage: `AssistantEngineTest.debugNormalizedCommandUsesNormalizerOutput` proves debug `normalizedCommand` uses normalized slots from `SlotNormalizer`, not only raw NLU slots.
- Fixed ASR eval artifact run: `WhisperNativeSmokeTest.nativeWhisperWritesFixedAsrEvaluationArtifact` passed on Pixel 10 and wrote `files/whisper-asr-eval.jsonl` with `timer-command.wav` -> transcript `поставь таймер на 5 минут.`, latency `2499 ms`, intent `set_timer`, and widget `timer_card`.
- Broader ASR eval preparation run: `tools/generate_asr_eval_audio.sh` generated four 16 kHz mono PCM WAV fixtures with the Russian `Milena` voice for weather, timer, calculator, and note commands, and copied them into `app/src/androidTest/assets/asr_eval/` with `manifest.jsonl`. `WhisperNativeSmokeTest.nativeWhisperWritesManifestAsrEvaluationArtifact` consumes that manifest and writes one `whisper-asr-eval.jsonl` row per case; the later connected run passed `4/4`.
- UI demo run: `DemoVideoFlowTest.recordVerticalDemoFlow` passed on Pixel 10. The flow covers weather, timer, note, and a complex local-model answer, and explicitly waits for `generic_answer_card` after the complex question. Previous screen recording evidence is at `build/device-screenshots/offline-assistant-demo-qwen25-32k-dedup.mp4`.
- Fresh UI demo video run for the requested phone demo path: `./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.offlineassistant.app.DemoVideoFlowTest` passed on Pixel 10 and screen recording evidence was captured at `build/device-screenshots/offline-assistant-demo-points1-2026-07-09.mp4`. The recorded flow covers weather, timer, note, and a complex local Qwen answer.
- Persistent Qwen warm-up run: `LlamaNativeSmokeTest.nativeLlamaCanWarmUpInstalledGgufBeforeGeneration` passed on Pixel 10. `MainChatScreen` starts a background warm-up that loads the GGUF into a process-lifetime native model cache.
- Fixed command evaluation set: `CommandEvaluationSetTest` passes and enforces `core/src/test/resources/eval/command_eval.jsonl` for 25 current time, weather, timer, timer clarification, alarm, alarm clarification, note, reminder, calculator, open app, help, and unknown/generic answer contracts. The expanded paraphrase set added `Сколько сейчас времени?`, `Который час сейчас?`, `Покажи погоду в Санкт-Петербурге`, `Запусти таймер на 10 минут`, `Поставь таймер на час`, `Будильник на 08.15`, `Разбуди меня завтра`, `Создай заметку проверить отчет`, `Создай напоминание через час позвонить маме`, `Сколько будет 18 плюс 24`, `Вычисли 100 минус 7`, `Запусти YouTube`, and `Что ты умеешь?`.
- Generic NLU rule-path improvements from the expanded eval: time intent now tolerates words between `сколько` and `времени`, help recognizes `что ты умеешь`, and reminder text extraction supports `создай напоминание` / `напоминание` in addition to `напомни`.
- Latest stabilization verification after Qwen answer-only reroute, action copy/open-app tests, and real-Whisper ASR debug coverage: `./gradlew test assembleDebug` passed locally, targeted `WhisperNativeSmokeTest` passed 2/2 on Pixel 10, then `./gradlew :app:connectedDebugAndroidTest` passed 28/28 on Pixel 10 with 0 skipped and 0 failures.
- After reinstalling the latest APK, UIAutomator dump confirmed `Android App Compatibility` is absent and the launched screen contains `Assistant` and `Напишите команду или нажмите микрофон.`.
- Notification permission unit run: `ChatViewModelFallbackTest.reminderCommandShowsNotificationPermissionCardWhenPostNotificationsMissing` proves a reminder command shows `PermissionCard` with `POST_NOTIFICATIONS` when the platform adapter reports that notification permission is missing.
- Notification permission UI stabilization run: after gating reminder cards on `POST_NOTIFICATIONS`, `./gradlew test assembleDebug` passed locally and targeted `./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.offlineassistant.app.MainChatScreenTest,com.offlineassistant.app.widgets.PermissionCardRendererTest` passed 9/9 on Pixel 10. Reminder-card UI tests now grant notification permission explicitly; `PermissionCardRendererTest` verifies the allow action carries `POST_NOTIFICATIONS` in the widget payload.
- Fresh full device run after notification permission stabilization: `./gradlew :app:connectedDebugAndroidTest` passed 29/29 on Pixel 10 with 0 skipped and 0 failures.
- Reminder notification scheduling is now wired from `ChatViewModel` to `AndroidPlatformAdapters` and `ReminderNotificationScheduler` when a `ReminderCard` is returned and `POST_NOTIFICATIONS` is granted.
- Notification permission acceptance run: after `adb shell pm revoke --user 0 com.offlineassistant.poc.debug android.permission.POST_NOTIFICATIONS` and clearing permission flags, direct instrumentation `adb shell am instrument -w -r --user 0 -e class com.offlineassistant.app.NotificationPermissionAcceptanceTest com.offlineassistant.poc.debug.test/androidx.test.runner.AndroidJUnitRunner` passed `OK (1 test)`. It clicked the app `PermissionCard`, accepted the Android system permission dialog, sent another reminder command, scheduled a local reminder notification, opened the notification shade, and found the reminder text.
- Notification permission screen recording evidence is at `build/device-screenshots/offline-assistant-notification-permission-2026-07-09.mp4`.
- Fresh full device run after adding notification acceptance test and scheduler wiring: `./gradlew :app:connectedDebugAndroidTest` completed successfully on Pixel 10. The normal suite skipped `NotificationPermissionAcceptanceTest` unless pre-run with permission revoked, and all non-skipped tests passed.
- Note edit implementation run: `ChatViewModelFallbackTest.noteEditActionLoadsExistingNoteIntoInputAndSendUpdatesPersistedNote` passed and proves `NoteCard` edit loads the existing note into chat input, `Send` updates the same persisted note, and the assistant returns an updated `NoteCard`. Targeted `MainChatScreenTest` passed 8/8 on Pixel 10, including `noteEditActionUpdatesPersistedStore`.
- Live human voice test: manual phone testing confirmed that voice input can drive the real Whisper -> RuBERT -> widget path when the transcript is accurate. One observed inaccurate transcript for a calculator command failed safely with an error card; a repeated clearer utterance produced the calculator widget correctly.
- ASR cleanup decision: one-off corrections for a single bad transcript are intentionally not part of the product path. Future fixes should improve Whisper settings/model selection, collect representative voice eval data, or add general language normalization rules that are valid across paraphrases.
- Fresh full connected verification after the ASR cleanup/docs update: `./gradlew :app:connectedDebugAndroidTest` passed on Pixel 10. The preconditioned notification acceptance test was skipped in the normal permission-granted run; all non-skipped tests passed.
- Open-app alternatives run: `ChatViewModelFallbackTest.openAppCommandAddsAlternativesWhenSeveralLaunchableAppsMatch` passed and proves multiple launchable app matches are attached to `OpenAppCard` as `alternatives`. `OpenAppCardRendererTest.alternativeButtonCarriesSelectedPackagePayload` passed on Pixel 10 and proves choosing an alternative emits the selected package/app name. `AndroidPlatformAdaptersTest.opensLaunchableAppByPackageNameAndLabel` passed on Pixel 10 and proves PackageManager lookup can find the installed app by label/package.
- Timer/alarm Android action wiring run: `ChatViewModelFallbackTest.timerCommandDelegatesToAndroidSystemTimerAndReturnsPassiveCard`, `alarmCommandDelegatesToAndroidSystemAlarmAndReturnsPassiveCard`, `timerCommandReturnsErrorCardWhenAndroidSystemTimerFails`, and `alarmOpenSystemWidgetActionOpensSystemAlarmScreen` passed locally. `AndroidPlatformAdapters` now implements timer/alarm delegation with `AlarmClock.ACTION_SET_TIMER` and `AlarmClock.ACTION_SET_ALARM`; it also opens the system alarm screen from the alarm card with `AlarmClock.ACTION_SHOW_ALARMS`. Timer cards with `mode=system_passive` render as passive cards without pause/cancel controls. Local `./gradlew test :app:compileDebugAndroidTestKotlin assembleDebug` passed after this follow-up; physical-device confirmation is pending until the phone is reconnected.
- Help/clarification widget interaction run: `ChatViewModelFallbackTest.helpExampleActionInsertsTextIntoInput` and `clarificationSuggestionActionInsertsTextIntoInput` passed locally. Help examples and clarification suggestions now emit widget actions carrying the selected text, and the ViewModel inserts that text into the chat input. Local `./gradlew test :app:compileDebugAndroidTestKotlin assembleDebug` passed; physical-device confirmation is pending until the phone is reconnected.
- Async voice UI stabilization run: `ChatViewModelFallbackTest.asyncVoiceRecordingStreamsFallbackAnswerAfterTranscript` passed and proves that after recording stops, the UI enters local recognition/processing state, appends the recognized voice transcript as a user bubble, streams local Qwen fallback tokens into one assistant bubble, and finalizes that same bubble without duplicating the answer.
- Async fallback failure stabilization run: `ChatViewModelFallbackTest.asyncTextFallbackFailureRendersErrorAndUnblocksUi` passed and proves an unexpected fallback/LLM exception renders `ErrorCard` and clears `isProcessing`.
- Native Qwen repeatability run: `LlamaNativeSmokeTest.llamaFallbackParserHandlesTwoSequentialStreamingAnswers` passed on Pixel 10 and proves two sequential streaming Qwen requests through the same JNI bridge/model cache do not crash the app process. The native bridge now uses RAII cleanup for llama contexts and samplers so callback/decode exceptions cannot leak those resources.
- Qwen UI repeatability run: after reinstalling the debug APK and copying `offline-assistant-qwen.gguf` into app files, `QwenUiSmokeTest.repeatedComplexQuestionsRenderTwoLocalLlmAnswerCards` passed on Pixel 10 and proves two complex questions through the real Compose UI render two `generic_answer_card` responses without a repeat-call crash.
- No-Wi-Fi/cellular local-model run: after `adb shell svc wifi disable` and `adb shell svc data disable`, targeted connected tests passed on Pixel 10: `WhisperNativeSmokeTest.nativeWhisperWritesFixedAsrEvaluationArtifact`, `RubertCommandEvaluationTest.stagedRubertDrivesBroaderParaphraseSetThroughAssistantEngine`, and `QwenUiSmokeTest.repeatedComplexQuestionsRenderTwoLocalLlmAnswerCards`. This was followed by the stricter airplane-mode/no-VPN run below.
- Fresh launch after async voice/Qwen repeatability changes: `./gradlew :app:installDebug` installed the APK on Pixel 10, `RECORD_AUDIO` and `POST_NOTIFICATIONS` were granted, and `adb shell monkey -p com.offlineassistant.poc.debug 1` launched the app. UIAutomator confirmed `Assistant`, `Напишите команду или нажмите микрофон.`, `Mic`, `Введите команду`, and `Send`, with no `Android App Compatibility` dialog.
- Fresh full connected verification after async voice and native Qwen repeatability changes: `./gradlew :app:connectedDebugAndroidTest` passed on Pixel 10. Gradle reported `Finished 35 tests on Pixel 10 - 16` and `BUILD SUCCESSFUL`; the two preconditioned permission acceptance tests were skipped in the normal permission-granted run.
- Post-suite manual-test setup: because the connected test run removed the debug package during cleanup, `./gradlew :app:installDebug` was run again, permissions were granted, and `adb shell monkey -p com.offlineassistant.poc.debug 1` relaunched the app. UIAutomator again confirmed the chat screen with `Assistant`, welcome text, `Mic`, input, and `Send`.
- Strict airplane-mode/no-VPN local-model run: with `airplane_mode_on=1`, `com.wireguard.android` temporarily disabled, and active `NetworkAgentInfo{...}` line count `0`, targeted connected tests passed 3/3 on Pixel 10: `WhisperNativeSmokeTest.nativeWhisperWritesFixedAsrEvaluationArtifact`, `RubertCommandEvaluationTest.stagedRubertDrivesBroaderParaphraseSetThroughAssistantEngine`, and `QwenUiSmokeTest.repeatedComplexQuestionsRenderTwoLocalLlmAnswerCards`. The generated XML report records `OK (3 tests)`.
- Post-offline restore/setup: airplane mode was disabled again, WireGuard was re-enabled, the debug APK was reinstalled because connected tests removed it, Qwen was copied back into app files, microphone/notification permissions were granted, and `MainActivity` was relaunched. UIAutomator confirmed `Assistant`, welcome text, `Mic`, input, and `Send`; model files in app storage are Qwen 469 MB, RuBERT 111 MB, and Whisper 57 MB.
- Blue Reference UI polish run: the main chat now uses a shared light theme, Material icon chrome, pale-blue user bubbles, assistant avatar, a unified mic/send/stop composer, compact localized transcript/processing state, and white bordered result cards. Settings now separates ASR selection, model readiness, RuBERT action threshold, and demo data without exposing filesystem paths. Widget actions no longer overflow at large display/font settings; source/state labels and timestamps are product-facing. Targeted `MainChatScreenTest` plus `SettingsScreenTest` passed 10/10 on Pixel 10 with `BUILD SUCCESSFUL` on 2026-07-10. Screenshots are stored under `build/device-screenshots/offline-assistant-ui-polish-*-2026-07-10.png`.
- Post-polish manual-test setup: `./gradlew :app:installDebug` reinstalled the debug APK, microphone/notification permissions were granted, and `MainActivity` was launched. UIAutomator confirmed the app is foregrounded at `com.offlineassistant.poc.debug/com.offlineassistant.app.MainActivity` and sees `Assistant`, one welcome bubble, `Mic`, `Введите команду`, and `Send`, with no `Android App Compatibility` or raw `debug:` text. Device app storage currently contains model files for Whisper, RuBERT, and Qwen. Screenshot evidence is at `build/device-screenshots/offline-assistant-blue-reference-ui-2026-07-09.png`.
- Device smoke runner follow-up: the connected Pixel run reproduced logcat eviction when all eval producers shared one suite. The runner now executes RuBERT/Qwen/Whisper producers separately and extracts each JSONL immediately. The final bundle contains host metrics, offline package-deny evidence, model evals, permission gates, demo video, screenshot and logcat and passes `scripts/verify_device_smoke_artifacts.py`.
- Live voice acceptance runner follow-up: an early 75-second capture without Mic interaction was intentionally rejected rather than replaced with synthetic audio. The later real human recording is accepted and documented below.
- Full acceptance runner follow-up: `scripts/run_full_acceptance.sh` is the single entrypoint for final verification. It runs host preflight, then `scripts/device_smoke_test.sh`, then `scripts/live_voice_acceptance.sh`, and verifies both artifact bundles. Host preflight now evaluates 12 RuBERT cases, requires adjacent aggregate metrics (exact `11/12`, strict regression `9/9` in the latest run), runs the native Qwen policy test, all script/static verifier tests, design checks, and Gradle unit/debug/release builds. Running without `--host-only` remains required for final connected acceptance.
- July 10 follow-up: the full runner initially reached the offline local-model gate on Pixel 10 but failed because the phone kept a WireGuard VPN `NetworkAgentInfo` active after Wi-Fi/mobile-data toggles. The runner now proves offline acceptance by enabling Android's OEM deny network chain and recording `package_networking_com.offlineassistant.poc.debug=com.offlineassistant.poc.debug:deny` while local Whisper/RuBERT/Qwen gates run, then restores package networking. The old `active_network_count=0` requirement is intentionally no longer used for normal device smoke because real phones can keep VPN/system networks alive.
- July 10 follow-up: `RubertCommandEvaluationTest` was split back to the intended architecture boundary. The broader RuBERT suite now contains only action/help commands and includes a guard that rejects unknown/local-LLM cases; complex local-model answers remain covered by `QwenAnswerEvaluationTest` and `QwenUiSmokeTest`. Targeted Pixel verification passed `RubertCommandEvaluationTest` 3/3. A later full acceptance retry stopped at `:app:installDebug` because adb lost device `59021FDCR003LL`; `scripts/run_full_acceptance.sh --host-only` passed immediately afterward.
- Full acceptance wait-mode follow-up: `scripts/run_full_acceptance.sh --wait-for-device` runs the same host preflight and then waits for adb to report a connected Android device before starting `device_smoke_test.sh` and `live_voice_acceptance.sh`. The wait mode is covered by `scripts/test_full_acceptance_script.py`; the connected stages were subsequently completed on Pixel 10.
- Acceptance status inspector follow-up: final evidence reports `host_preflight: present`, `device_smoke: present`, `live_voice: present`, and `complete: true`.
- Final DoD status follow-up: the 40-requirement matrix has repository, host, device and live-human voice evidence; all requirements are proven.
- Core skill-registry follow-up: all MVP action/help handlers were extracted into concrete Android-free skills under `:core`; `AssistantEngine` now dispatches normalized commands through `SkillRegistry`. `BuiltInSkillsTest` covers complete intent ownership, duplicate rejection, representative payloads and real engine dispatch.
- RuBERT aggregate-metrics follow-up: `training/rubert/evaluate_export.py` now evaluates all 12 fixed cases and writes both JSONL rows and adjacent metrics JSON. The latest host result is intent accuracy `1.000`, macro F1 `1.000`, slot precision `0.828`, slot recall `0.923`, slot F1 `0.873`, validation pass rate `0.917`, exact `11/12`, and strict regression gate `9/9`. Acceptance scripts require the metrics artifact.
- Runtime telemetry follow-up: Whisper transcription, RuBERT inference and Qwen warm-up/generation now persist their latest operation, success/failure, latency and error. Settings and Debug display this separately from file readiness. Production Qwen warm-up moved to `OfflineAssistantApp`, so it also runs when `MainChatScreen` receives the production-injected ViewModel.
- Qwen host stabilization follow-up: model capacity remains `32768` context and `8192` generation tokens. Normal answers are requested as 2-4 sentences; native generation stops at a semantic boundary, has a 900-character emergency cap and a 90-second timeout. `python3 scripts/test_qwen_generation_policy.py`, app unit tests and `assembleDebug` pass. The pre-TTFT-optimization Pixel run measured `11821-22872 ms` across `12/12` accepted cases; later optimized results are recorded below.
- Acceptance artifact lifecycle follow-up: Qwen/RuBERT/Whisper connected JSONL rows are emitted through stable logcat tags and collected immediately after their producer suite. The runner no longer depends on app-private files surviving subsequent instrumentation teardown.
- Connected model results before the Whisper upgrade: RuBERT slot-only passed `8/8`; Whisper Base Q5_1 passed `4/4` expected intent/widget cases at `3490-3828 ms`; Qwen passed `12/12` quality cases at `11821-22872 ms` (median `13369 ms`, mean `14710 ms`).
- Android Clock integration follow-up: `DemoVideoFlowTest` exposed a wrong permission name plus foreground handoff. The manifest now requests `com.android.alarm.permission.SET_ALARM`, timer/alarm intents set `EXTRA_SKIP_UI=true`, `AndroidPlatformAdaptersTest` passes `3/3`, and the recorded demo flow passes with the assistant staying foregrounded and showing the passive timer card.

Expected manual checks:

- app launches on connected Pixel;
- chat input accepts text;
- `Поставь таймер на 5 минут` renders assistant text and `TimerCard`;
- `Какая погода сегодня?` renders `WeatherCard` with `21°`;
- `Посчитай 125 умножить на 37` renders `CalculatorCard` with `4625`;
- `Напомни через час проверить духовку` renders `ReminderCard`;
- `Открой <app>` renders `OpenAppCard`; if multiple launchable apps match, the card shows explicit alternatives and opens the selected package.
- top bar and bottom navigation match the reference shell contract: `Assistant`, menu, privacy status, `Чат`, `История`, `Навыки`, `Настройки`, separate voice action;
- widget preview tab shows all required cards;
- settings tab reports Whisper/RuBERT/Qwen model readiness;
- debug history is visible after a command on the `История` tab and shows transcript, intent, fallback status, action result, slots, normalized command, and latency fields for ASR placeholder, NLU, fallback LLM, normalization, skill execution, and total.
- Android `SharedPreferences` note/reminder stores persist records across store instances.
- Reminder notification scheduler creates the local notification channel and schedules an alarm-backed reminder without crashing.
- Timer, alarm, reminder, and note widget buttons emit visible chat feedback. The alarm card's system-open button now routes to the Android platform adapter. When timer creation is delegated to Android system timer, the passive card hides in-app pause/cancel controls instead of pretending the app can control the system timer.
- Settings follow-up: `AssistantSettingsRepository` persists the fallback threshold, `SettingsScreen` exposes the threshold slider and demo cleanup buttons, and `ChatViewModel` reads the current threshold through a provider on each request. `ChatViewModelFallbackTest.fallbackThresholdProviderChangesRoutingWithoutRecreatingViewModel`, `clearChatHistoryKeepsOnlyWelcomeMessageAndClearsDebugHistory`, and `clearNotesAndRemindersDeletesPersistedItems` pass locally.
- Missing-model/error-card follow-up: `AssistantEngineTest.fallbackErrorRendersErrorCardInsteadOfGenericAnswer` proves `FallbackKind.ERROR` returns `ResponseStatus.ERROR` with `error_card` and a recoverable "Открыть настройки" suggestion. `ErrorCardRendererTest.suggestionButtonCarriesSettingsTargetPayload` proves the suggestion renders as an action with `target=settings`, and `ChatViewModelFallbackTest.errorSuggestionSettingsActionShowsNavigationFeedback` covers local feedback. `MainChatScreen` routes that action to the Settings tab in app navigation; subsequent connected suites passed.
- Debug readiness follow-up: `DebugScreen` renders model readiness alongside command diagnostics, using `ModelReadinessRepository` at runtime and injectable rows for tests. `DebugScreenTest.showsModelReadinessSection` covers ready/missing rows; the final live-human run also captured real Whisper/RuBERT runtime telemetry and stage latency in this screen.
- Training layout follow-up: the spec-facing `training/data`, `training/scripts`, and `training/README.md` deliverables now exist while preserving the maintained `training/rubert` implementation. `python3 training/test_training_layout.py` passes and checks required files, eval slot records, and metric names. Wrapper help for `training/scripts/train_rubert_tiny2.py` and `training/scripts/evaluate_nlu.py` resolves to the existing training/evaluation entry points.
- Internal navigation follow-up: `OfflineAssistantApp` now uses `BuildConfig.DEBUG` to expose internal tabs only in debug builds. `OfflineAssistantNavigationTest` proves debug navigation includes `История` and `Навыки`, while release-style navigation keeps only `Чат` and `Настройки`. Fresh local verification included `:app:compileReleaseKotlin`.
- Voice privacy follow-up: recorder-generated cache WAV files named `voice-command-*.wav` are deleted after sync and async voice processing, while staged ASR/eval fixtures are not treated as recorder output. `ChatViewModelFallbackTest.voiceRecordingDeletesRecorderCacheFileAfterProcessing` and `asyncVoiceRecordingDeletesRecorderCacheFileAfterProcessing` pass locally. Fresh local verification: `python3 training/test_training_layout.py && ./gradlew test :app:compileDebugAndroidTestKotlin assembleDebug :app:compileReleaseKotlin`.
- Weather provider follow-up: weather data now enters the assistant through the pure Kotlin `WeatherProvider` contract. The default `MockWeatherProvider` preserves the visible `mock` source, and `AssistantEngineTest.weatherCommandUsesInjectedWeatherProvider` plus `:core:test` pass locally for injected cache-style weather data.
- Qwen eval follow-up: the real-device Qwen answer eval now uses shared `QwenAnswerEvalCases` with 12 fixed Russian complex/general questions across concept, offline-limits, health, latency, voice-help, privacy, safety, choice, planning, troubleshooting, recency, and device-limits categories. `QwenAnswerEvalCasesTest.fixedQuestionsCoverBroaderComplexQuestionCategories` passes locally and `QwenAnswerEvaluationTest` compiles against the shared list. A connected Pixel rerun is still required to generate the expanded GGUF latency/quality artifact.
- Widget preview follow-up: `WidgetPreviewScreenTest.sampleWidgetsCoverAllExpectedWidgetTypesInOrder` proves the internal preview includes deterministic payloads for all expected widget types, including `GenericAnswerCard`. `openAppPreviewIncludesAlternativeSelectionPayload` proves the OpenApp preview covers alternative selection with multiple app/package options.
- Core skill-contract follow-up: `SkillContractsTest.skillReceivesNormalizedCommandAndReturnsStructuredResult` proves the reusable Android-free contracts `NormalizedCommand`, `Skill`, `SkillResult`, and `SkillStatus` exist in `:core`, carry widget payloads, and serialize `SkillStatus` as stable lower-snake product values.
- Slot-normalizer follow-up: `SlotNormalizerTest` proves the reusable Android-free `SlotNormalizer` contract returns `NormalizedCommand` for complete NLU and deterministic `ClarificationRequest` for incomplete timer commands, with `ValidationError` available for non-clarifiable validation failures. `AssistantEngineTest.engineUsesInjectedSlotNormalizerForValidationClarification` proves `AssistantEngine` calls the injected normalizer before high-confidence skill execution.
- Intent-schema follow-up: `IntentSchemaTest.defaultIntentSchemaDefinesMvpIntentsAndWidgetMappings` proves `IntentSchema.default` defines MVP intent ids, required slots, optional slots, skill ids, and success widget mappings. `DeterministicSlotNormalizer` now reads required slots from this schema.
- If `RECORD_AUDIO` is not granted, tapping the microphone shows a `PermissionCard` explaining why microphone access is needed.
- If `POST_NOTIFICATIONS` is not granted, reminder commands show a `PermissionCard`; the rendered allow action carries the requested permission from the widget payload to the runtime permission launcher.
- The app now has a real `AudioRecord` recorder that writes 16 kHz mono PCM into a WAV file for the local ASR adapter.
- A unit-tested voice command pipeline converts a transcript such as `Поставь таймер на 5 минут` into the existing timer intent and `TimerCard`.
- The microphone start/stop UI now routes through `AudioRecord` -> `WhisperTranscriber` -> voice command pipeline.
- After recording stops, the microphone UI now uses an async pipeline rather than blocking the Compose click handler; complex recognized questions use the same streaming Qwen answer path as typed text.
- The debug APK now packages native `arm64-v8a` `offlineassistant_whisper` and `whisper` libraries built from vendored `whisper.cpp`.
- On Pixel 10, native `WhisperTranscriber` loads the app-file Whisper model, transcribes a Russian timer WAV, and the transcript drives `TimerCard`.
- `WhisperNativeSmokeTest.nativeWhisperVoiceRecordingAddsAsrLatencyToDebugHistory` passed on Pixel 10 and proves the real Whisper -> `ChatViewModel.handleVoiceRecording` path appends a voice user message, renders `TimerCard`, and records ASR latency in debug history.
- Historical Small comparison build bundled `models/whisper/whisper-small-multilingual-q5_1.bin`; the current build removes it after the Pixel benchmark showed an unfavorable size/latency tradeoff.
- Manual model staging is not required for Whisper readiness. Small Q5_1 is `181 MiB`; the resulting debug APK is approximately `279 MiB` (`295 MB` decimal on disk).
- Native shared libraries are linked with 16 KB page-size compatible ELF `LOAD` segment alignment.
- ONNX Runtime Android is linked into the app; `OnnxRubertNlu` routes to ONNX when a complete RuBERT bundle is ready and otherwise falls back to rule-based NLU.
- RuBERT readiness now requires a complete bundle: `rubert-tiny2-intent-slots.onnx`, `vocab.txt`, `intent_labels.txt`, and `slot_labels.txt`.
- Qwen local answers are wired from `MainChatScreen` -> `ChatViewModel` -> `AssistantEngine`; it now has a llama.cpp JNI bridge and can run when a compatible GGUF is installed in app files.
- Qwen is warmed in the background after chat launch when the GGUF is ready; warm-up now creates both the cached native model and one persistent `32768` context. Serialized requests clear llama memory metadata and reuse that context with prompt batch `512` instead of reallocating it.
- Sequential Qwen streaming requests are covered by native-parser and real-UI connected Pixel 10 smoke tests.
- The transcript preview shows one localized UI prefix only: `Распознано: <текст>` (or `Слушаю...` before a partial is available). `TranscriptPreviewLabelTest.transcriptPreviewLabelAddsExactlyOnePrefix` proves the helper also strips the legacy English prefix instead of duplicating it.

Final connected acceptance and residual constraints:

- Live human Mic -> Whisper -> RuBERT -> timer evidence is saved under `build/device-smoke/offline-assistant-live-voice*`. Whisper produced `поставь в таймер на 5 минут`; RuBERT selected `set_timer` with confidence `0.983`, fallback `false`, and the app rendered the valid passive system `TimerCard`. Debug history records ASR `1975 ms`, NLU `618 ms`, skill `1 ms` and total `2594 ms`.
- The final RuBERT artifact passes strict Pixel slot-only evaluation `8/8`; host aggregate evaluation is exact `11/12`, regression `9/9`, intent accuracy/macro F1 `1.000`, slot F1 `0.873`.
- The expanded Qwen real-model gate still passes all `12/12` safe plain-text cases after TTFT optimization. The gate now separately requires both a denial and a cache/last-saved alternative for fresh offline weather/currency questions. Complete-answer latency remains variable (roughly `5-29 s` in measured optimized runs); answer length still dominates completion time even though first-token latency is substantially lower.
- The original Base Q5_1 connected manifest passed all `4/4` weather/timer/calculator/note intent/widget cases at `3490-3828 ms`; the shorter live human timer recording completed ASR in `1975 ms`. Selected Small Q5_1 also passes `4/4`, at `8305-15410 ms` (median `13615.5 ms`, mean `12736.5 ms`), with approximately `450 MB PSS` / `537 MB RSS` peak process memory. Recognition differences remain visible and are not patched by phrase-specific fallbacks.
- Whisper Base/Small comparison artifacts are stored at `build/device-smoke/whisper-asr-eval-base-q5_1.jsonl` and `build/device-smoke/whisper-asr-eval-small-q5_1.jsonl`. Both route `4/4`; Small is approximately `3.5x` slower by median on this Pixel 10 run.
- `scripts/verify_device_smoke_artifacts.py --live-voice`, `scripts/acceptance_status.py` and `scripts/final_dod_status.py` all pass; the latter reports all 40 DoD requirements proven.
- ASR selector follow-up: Settings now persists and displays Whisper Base Q5_1, Zipformer RU INT8 and T-one RU Streaming. Whisper Small is retained only as historical benchmark evidence and removed from the APK because it was both 181 MiB and substantially slower. Model bundles are materialized off the Compose main thread, and `AudioTranscriberFactory` routes recording through the selected backend.
- Zipformer RU INT8 connected evaluation decoded weather/timer/calculator/note WAVs in `134/41/38/41 ms` after warm-up. Transcripts were semantically correct; weather, timer and note completed the existing widget path. Calculator produced word numerals (`восемнадцать`, `три`), which identifies a generic ITN/slot-normalization gap rather than an ASR phrase to patch.
- T-one RU streaming connected smoke passed on Pixel 10: PCM chunks emitted changing partial transcripts and `finish()` published the final transcript without a second ASR run. Partial text is preview-only; RuBERT receives only the final transcript.
- Product default was subsequently changed to T-one RU Streaming; Whisper Base remains selectable and persisted explicit user choices are preserved.
- Baseline warmed Qwen visible TTFT on Pixel 10 was `4104`, `3801` and `7222 ms` (median `4104 ms`). With the final strengthened prompt policies, persistent `32768` context reuse, prompt batch `512` and compact topic guidance reduced the same benchmark to `1321`, `2065` and `4077 ms` (median `2065 ms`, about `50%` lower). Native phase logs show prompt prefill, rather than context creation/reset, now dominates TTFT.
- The latency tradeoff is memory: production warm-up with the persistent context measured approximately `1.35-1.45 GB` total PSS on Pixel 10 versus the earlier roughly `971 MB` co-residency measurement. Batch `512` itself added little compared with the full 32K context; a lower-memory device matrix remains required before production use.
- Long Qwen evaluation exposed a JNI crash when one token ended with the first byte of a Cyrillic UTF-8 code point. Native streaming now buffers split sequences across token callbacks, converts complete code points to UTF-16 with `NewString`, replaces only an incomplete final tail, and has host coverage for split Cyrillic and supplementary characters. The full `12/12` quality run and repeated Compose Qwen smoke pass after the fix.
- Warmed Whisper Base thread benchmark on the same Pixel/fixture: 4 threads `[2202, 2477, 2223] ms` (median `2223`), 6 threads `[2271, 3013, 3395] ms` (median `3013`), 8 threads `[11819, 11595, 12724] ms` (median `11819`). Four threads remain selected. The active whisper context is now cached and background-warmed instead of reloaded for each message.
- Vulkan experiment: the opt-in `-PasrVulkan=true` API-28 build compiled, included `libggml-vulkan.so`, and passed 16 KB zip alignment. Its first timer fixture took about 25 seconds and failed transcript correctness, so the experiment was rejected and the shipped build remains CPU-only.
- Final clean CPU build after removing Whisper Small is approximately `271 MiB`; a clean install materialized Base/RuBERT/Qwen without readiness races. Settings reported all three active runtime models `ready`, and the voice-model popup showed Base, Zipformer RU INT8 and T-one RU Streaming on the Pixel screen.

Project-wide UX optimization follow-up (2026-07-13):

- P0 responsiveness is implemented: recorder finalization is asynchronous, T-one does not create a normal-path WAV, Qwen publishes 40 ms UI batches, native generation is cancellable, the composer remains editable and auto-scroll disengages when the user reads older messages.
- T-one trailing-silence endpoint detection passed the connected smoke after adding a generic 1.1-second RMS detector alongside sherpa endpoint state. Stable and mutable partial transcript segments are rendered separately; only the final transcript enters NLU.
- Repeated Qwen plus Silero, manual replay and Qwen Stop/UI responsiveness pass on Pixel. TTS playback now retains `AudioTrack` through audible PCM completion and reports first-audible latency.
- RuBERT cached-runtime benchmark preserved all four tested intents. Medians were CPU/2 `7.04 ms`, CPU/4 `9.71 ms`, CPU/6 `7.00 ms`, XNNPACK `12.74 ms`, NNAPI `10.48 ms`; production now uses CPU/2.
- Generic Russian cardinal-number/spoken-time ITN converts examples such as `восемнадцать умножить на три`, `двадцать пять минут` and `семь тридцать` without transcript-specific command rewrites. Core/app unit tests pass.
- The normal timer path is now an in-app persisted countdown with pause/resume/cancel. History is bounded to 100 persisted messages, reminders are rescheduled after reboot/package/time changes, and weather has explicit mock/cache fallback semantics.
- Pixel-generated app-only Baseline/Startup Profiles contain 1,556/655 lines. The release APK contains compiled dexopt profiles and builds with R8/resource shrinking at about 259 MiB.
- Five cold Macrobenchmark iterations measured `347.71 ms` startup median without compilation and `341.26 ms` with the Baseline Profile. The chat-action run and all 15 Perfetto traces are retained under `benchmark/build/outputs/connected_android_test_additional_output`.
- The remaining optimization experiments are full date/ordinal ITN, opt-in noisy real-speech plus NS/AGC comparison, Qwen prefix/KV/context matrix, and broader sustained thermal/lower-memory device coverage. They are not enabled without correctness measurements.
- Fresh full-stack residency measured about `1.73 GiB PSS` / `1.85 GiB RSS`. A forced `RUNNING_LOW` callback released the persistent llama context and reduced the live process to `1.16 GiB PSS` without a crash; foreground rewarm recreated it in `106 ms` and settled near `1.65 GiB PSS`. Thermal status remained `0`.
- The final optimized debug APK is installed and foregrounded on Pixel 10. App-private model directories are Qwen `469 MiB`, RuBERT `112 MiB`, T-one `138 MiB` and Silero `131 MiB`; screenshot evidence is `build/device-screenshots/ux-optimized-final-2026-07-13.png`.
- Manual speaker replay in the final installed build synthesized the welcome text in `399 ms` and recorded first audible PCM at `437 ms`; AudioTrack focus was acquired/released and the Android crash buffer remained empty. Automatic speech is enabled by default for a clean installation.
- Early Qwen speech follow-up: `SpeechChunker` emits source-addressable `PlannedSpeechChunk` values, accepts safe clause punctuation after 28 characters when queued/active speech is below 1,400 ms, and uses a word boundary near 64 characters only before the first spoken phrase. Continuations prefer punctuation and have a 180-character emergency guard. The first threshold was tightened after a real answer waited 2,698 ms for first audible PCM because its 45-character opening clause missed the former 50-character limit; the same answer then started in 1,390 ms. Synthesis and playback remain separate ordered stages with one-chunk rendezvous prefetch, while all response chunks now feed one persistent streaming `AudioTrack` and one audio-focus lease.
- The synthesis/playback concurrency test passed `5/5`. `SileroNativeSmokeTest` passed on Pixel with a trailing-comma early clause, synthesizing `Солнечный свет состоит из множества разных видимых цветов,` in `368 ms` into `3,362 ms` of valid PCM.
- Adaptive speech/playback follow-up passed all core/app JVM tests plus debug/release assembly. On Pixel 10, `AudioTrackPcmPlayerTest` passed `2/2`, including ordered callbacks for two half-second chunks in one response-level playback session; `SileroNativeSmokeTest` passed in `6.898 s`, and `SileroChatIntegrationTest` passed in `9.338 s`. `QwenUiSmokeTest.userCanStopStreamingSpeechWithoutLaterTokensRestartingIt` passed in `20.204 s`: the real Qwen stream exposed phrase playback state, per-message Stop cancelled audio without stopping text generation, and later deltas did not restart TTS. Phrase highlighting uses the same exact source range. No PCM trimming or crossfade was introduced.
